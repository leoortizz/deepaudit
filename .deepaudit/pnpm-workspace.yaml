# Sever this directory from any ancestor pnpm workspace. pnpm walks up
# until it finds a pnpm-workspace.yaml and uses that directory as the
# workspace root — having one here means `pnpm install` from `.deepaudit/`
# manages only `.deepaudit/`, no matter what the parent repo defines.
packages: []
